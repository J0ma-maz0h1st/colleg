info:
  name: Applications Create Request
  type: http
  seq: 8

http:
  method: GET
  url: "{{BaseURL}}users/application/create/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
