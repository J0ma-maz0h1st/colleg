info:
  name: Change Password Request
  type: http
  seq: 2

http:
  method: GET
  url: "{{BaseURL}}users/change-password/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
