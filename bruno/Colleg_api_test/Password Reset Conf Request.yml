info:
  name: Password Reset Conf Request
  type: http
  seq: 6

http:
  method: POST
  url: "{{BaseURL}}users/password-reset-confirm/"
  body:
    type: json
    data: |-
      {

          "uid": "Mg",
          "token": "d7ujtx-81a53557c37fc69f69aacacf9697721a",
          "new_password": "12345"

      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
